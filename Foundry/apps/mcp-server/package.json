{
  "name": "@foundry/mcp-server",
  "version": "0.1.0",
  "private": true,
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "mcp-server": "node dist/server.js",
    "lint": "echo 'MCP lint not configured yet'"
  },
  "dependencies": {
    "@foundry/database": "workspace:*",
    "@foundry/domain": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "dotenv": "^16.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
